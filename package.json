{
  "name": "ts-ref",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "prebuild": "rm -rf lib",
    "build": "tsc -p src/tsconfig.json"
  },
  "dependencies": {
    "typescript": "^3.3.3333"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.3.4",
    "@babel/preset-typescript": "^7.3.3",
    "@types/jest": "^24.0.10",
    "babel-jest": "^24.3.1",
    "jest": "^24.3.1"
  }
}
